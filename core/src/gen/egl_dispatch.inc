// Generated by tools/gen_egl.py. Do not edit.
bool zbegl_manual_eglChooseConfig(HostEgl& host, HostEgl::Call& call);
bool zbegl_manual_eglCreateContext(HostEgl& host, HostEgl::Call& call);
bool zbegl_manual_eglCreateImage(HostEgl& host, HostEgl::Call& call);
bool zbegl_manual_eglCreatePbufferFromClientBuffer(HostEgl& host, HostEgl::Call& call);
bool zbegl_manual_eglCreatePbufferSurface(HostEgl& host, HostEgl::Call& call);
bool zbegl_manual_eglCreatePixmapSurface(HostEgl& host, HostEgl::Call& call);
bool zbegl_manual_eglCreatePlatformPixmapSurface(HostEgl& host, HostEgl::Call& call);
bool zbegl_manual_eglCreatePlatformWindowSurface(HostEgl& host, HostEgl::Call& call);
bool zbegl_manual_eglCreateSync(HostEgl& host, HostEgl::Call& call);
bool zbegl_manual_eglCreateWindowSurface(HostEgl& host, HostEgl::Call& call);
bool zbegl_manual_eglGetConfigAttrib(HostEgl& host, HostEgl::Call& call);
bool zbegl_manual_eglGetConfigs(HostEgl& host, HostEgl::Call& call);
bool zbegl_manual_eglGetCurrentContext(HostEgl& host, HostEgl::Call& call);
bool zbegl_manual_eglGetCurrentDisplay(HostEgl& host, HostEgl::Call& call);
bool zbegl_manual_eglGetCurrentSurface(HostEgl& host, HostEgl::Call& call);
bool zbegl_manual_eglGetPlatformDisplay(HostEgl& host, HostEgl::Call& call);
bool zbegl_manual_eglGetProcAddress(HostEgl& host, HostEgl::Call& call);
bool zbegl_manual_eglQueryContext(HostEgl& host, HostEgl::Call& call);
bool zbegl_manual_eglQueryString(HostEgl& host, HostEgl::Call& call);
bool zbegl_manual_eglQuerySurface(HostEgl& host, HostEgl::Call& call);
bool zbegl_manual_eglSwapBuffers(HostEgl& host, HostEgl::Call& call);

bool HostEgl::dispatch(Call& call) {
    switch (call.index()) {
    case ZB_EGL_HC_eglBindAPI: {
        const EGLenum api = call.scalar<EGLenum>(0);
        if (!call.valid()) return true;
        call.set_result(backend_.eglBindAPI(api));
        return true;
    }
    case ZB_EGL_HC_eglBindTexImage: {
        EGLDisplay dpy = call.handle<EGLDisplay>(0);
        if (!call.valid()) return true;
        EGLSurface surface = call.handle<EGLSurface>(1);
        if (!call.valid()) return true;
        const EGLint buffer = call.scalar<EGLint>(2);
        if (!call.valid()) return true;
        call.set_result(backend_.eglBindTexImage(dpy, surface, buffer));
        return true;
    }
    case ZB_EGL_HC_eglChooseConfig: {
        return zbegl_manual_eglChooseConfig(*this, call);
    }
    case ZB_EGL_HC_eglClientWaitSync: {
        EGLDisplay dpy = call.handle<EGLDisplay>(0);
        if (!call.valid()) return true;
        const EGLSync sync = call.scalar<EGLSync>(1);
        const EGLint flags = call.scalar<EGLint>(2);
        const EGLTime timeout = call.scalar<EGLTime>(3);
        if (!call.valid()) return true;
        call.set_result(backend_.eglClientWaitSync(dpy, sync, flags, timeout));
        return true;
    }
    case ZB_EGL_HC_eglCopyBuffers: {
        EGLDisplay dpy = call.handle<EGLDisplay>(0);
        if (!call.valid()) return true;
        EGLSurface surface = call.handle<EGLSurface>(1);
        if (!call.valid()) return true;
        const EGLNativePixmapType target = call.scalar<EGLNativePixmapType>(2);
        if (!call.valid()) return true;
        call.set_result(backend_.eglCopyBuffers(dpy, surface, target));
        return true;
    }
    case ZB_EGL_HC_eglCreateContext: {
        return zbegl_manual_eglCreateContext(*this, call);
    }
    case ZB_EGL_HC_eglCreateImage: {
        return zbegl_manual_eglCreateImage(*this, call);
    }
    case ZB_EGL_HC_eglCreatePbufferFromClientBuffer: {
        return zbegl_manual_eglCreatePbufferFromClientBuffer(*this, call);
    }
    case ZB_EGL_HC_eglCreatePbufferSurface: {
        return zbegl_manual_eglCreatePbufferSurface(*this, call);
    }
    case ZB_EGL_HC_eglCreatePixmapSurface: {
        return zbegl_manual_eglCreatePixmapSurface(*this, call);
    }
    case ZB_EGL_HC_eglCreatePlatformPixmapSurface: {
        return zbegl_manual_eglCreatePlatformPixmapSurface(*this, call);
    }
    case ZB_EGL_HC_eglCreatePlatformWindowSurface: {
        return zbegl_manual_eglCreatePlatformWindowSurface(*this, call);
    }
    case ZB_EGL_HC_eglCreateSync: {
        return zbegl_manual_eglCreateSync(*this, call);
    }
    case ZB_EGL_HC_eglCreateWindowSurface: {
        return zbegl_manual_eglCreateWindowSurface(*this, call);
    }
    case ZB_EGL_HC_eglDestroyContext: {
        EGLDisplay dpy = call.handle<EGLDisplay>(0);
        if (!call.valid()) return true;
        EGLContext ctx = call.handle<EGLContext>(1);
        if (!call.valid()) return true;
        if (!call.valid()) return true;
        call.set_result(backend_.eglDestroyContext(dpy, ctx));
        return true;
    }
    case ZB_EGL_HC_eglDestroyImage: {
        EGLDisplay dpy = call.handle<EGLDisplay>(0);
        if (!call.valid()) return true;
        const EGLImage image = call.scalar<EGLImage>(1);
        if (!call.valid()) return true;
        call.set_result(backend_.eglDestroyImage(dpy, image));
        return true;
    }
    case ZB_EGL_HC_eglDestroySurface: {
        EGLDisplay dpy = call.handle<EGLDisplay>(0);
        if (!call.valid()) return true;
        EGLSurface surface = call.handle<EGLSurface>(1);
        if (!call.valid()) return true;
        if (!call.valid()) return true;
        call.set_result(backend_.eglDestroySurface(dpy, surface));
        return true;
    }
    case ZB_EGL_HC_eglDestroySync: {
        EGLDisplay dpy = call.handle<EGLDisplay>(0);
        if (!call.valid()) return true;
        const EGLSync sync = call.scalar<EGLSync>(1);
        if (!call.valid()) return true;
        call.set_result(backend_.eglDestroySync(dpy, sync));
        return true;
    }
    case ZB_EGL_HC_eglGetConfigAttrib: {
        return zbegl_manual_eglGetConfigAttrib(*this, call);
    }
    case ZB_EGL_HC_eglGetConfigs: {
        return zbegl_manual_eglGetConfigs(*this, call);
    }
    case ZB_EGL_HC_eglGetCurrentContext: {
        return zbegl_manual_eglGetCurrentContext(*this, call);
    }
    case ZB_EGL_HC_eglGetCurrentDisplay: {
        return zbegl_manual_eglGetCurrentDisplay(*this, call);
    }
    case ZB_EGL_HC_eglGetCurrentSurface: {
        return zbegl_manual_eglGetCurrentSurface(*this, call);
    }
    case ZB_EGL_HC_eglGetDisplay: {
        const EGLNativeDisplayType display_id = call.scalar<EGLNativeDisplayType>(0);
        if (!call.valid()) return true;
        call.set_handle(backend_.eglGetDisplay(display_id));
        return true;
    }
    case ZB_EGL_HC_eglGetError: {
        if (!call.valid()) return true;
        call.set_result(backend_.eglGetError());
        return true;
    }
    case ZB_EGL_HC_eglGetPlatformDisplay: {
        return zbegl_manual_eglGetPlatformDisplay(*this, call);
    }
    case ZB_EGL_HC_eglGetProcAddress: {
        return zbegl_manual_eglGetProcAddress(*this, call);
    }
    case ZB_EGL_HC_eglGetSyncAttrib: {
        EGLDisplay dpy = call.handle<EGLDisplay>(0);
        if (!call.valid()) return true;
        const EGLSync sync = call.scalar<EGLSync>(1);
        const EGLint attribute = call.scalar<EGLint>(2);
        EGLAttrib * value = call.pointer<EGLAttrib>(3, 1u, kPageRead | kPageWrite);
        if (!call.valid()) return true;
        call.set_result(backend_.eglGetSyncAttrib(dpy, sync, attribute, value));
        return true;
    }
    case ZB_EGL_HC_eglInitialize: {
        EGLDisplay dpy = call.handle<EGLDisplay>(0);
        if (!call.valid()) return true;
        EGLint * major = call.pointer<EGLint>(1, 1u, kPageRead | kPageWrite);
        EGLint * minor = call.pointer<EGLint>(2, 1u, kPageRead | kPageWrite);
        if (!call.valid()) return true;
        call.set_result(backend_.eglInitialize(dpy, major, minor));
        return true;
    }
    case ZB_EGL_HC_eglMakeCurrent: {
        EGLDisplay dpy = call.handle<EGLDisplay>(0);
        if (!call.valid()) return true;
        EGLSurface draw = call.handle<EGLSurface>(1);
        if (!call.valid()) return true;
        EGLSurface read = call.handle<EGLSurface>(2);
        if (!call.valid()) return true;
        EGLContext ctx = call.handle<EGLContext>(3);
        if (!call.valid()) return true;
        if (!call.valid()) return true;
        call.set_result(backend_.eglMakeCurrent(dpy, draw, read, ctx));
        return true;
    }
    case ZB_EGL_HC_eglQueryAPI: {
        if (!call.valid()) return true;
        call.set_result(backend_.eglQueryAPI());
        return true;
    }
    case ZB_EGL_HC_eglQueryContext: {
        return zbegl_manual_eglQueryContext(*this, call);
    }
    case ZB_EGL_HC_eglQueryString: {
        return zbegl_manual_eglQueryString(*this, call);
    }
    case ZB_EGL_HC_eglQuerySurface: {
        return zbegl_manual_eglQuerySurface(*this, call);
    }
    case ZB_EGL_HC_eglReleaseTexImage: {
        EGLDisplay dpy = call.handle<EGLDisplay>(0);
        if (!call.valid()) return true;
        EGLSurface surface = call.handle<EGLSurface>(1);
        if (!call.valid()) return true;
        const EGLint buffer = call.scalar<EGLint>(2);
        if (!call.valid()) return true;
        call.set_result(backend_.eglReleaseTexImage(dpy, surface, buffer));
        return true;
    }
    case ZB_EGL_HC_eglReleaseThread: {
        if (!call.valid()) return true;
        call.set_result(backend_.eglReleaseThread());
        return true;
    }
    case ZB_EGL_HC_eglSurfaceAttrib: {
        EGLDisplay dpy = call.handle<EGLDisplay>(0);
        if (!call.valid()) return true;
        EGLSurface surface = call.handle<EGLSurface>(1);
        if (!call.valid()) return true;
        const EGLint attribute = call.scalar<EGLint>(2);
        const EGLint value = call.scalar<EGLint>(3);
        if (!call.valid()) return true;
        call.set_result(backend_.eglSurfaceAttrib(dpy, surface, attribute, value));
        return true;
    }
    case ZB_EGL_HC_eglSwapBuffers: {
        return zbegl_manual_eglSwapBuffers(*this, call);
    }
    case ZB_EGL_HC_eglSwapInterval: {
        EGLDisplay dpy = call.handle<EGLDisplay>(0);
        if (!call.valid()) return true;
        const EGLint interval = call.scalar<EGLint>(1);
        if (!call.valid()) return true;
        call.set_result(backend_.eglSwapInterval(dpy, interval));
        return true;
    }
    case ZB_EGL_HC_eglTerminate: {
        EGLDisplay dpy = call.handle<EGLDisplay>(0);
        if (!call.valid()) return true;
        if (!call.valid()) return true;
        call.set_result(backend_.eglTerminate(dpy));
        return true;
    }
    case ZB_EGL_HC_eglWaitClient: {
        if (!call.valid()) return true;
        call.set_result(backend_.eglWaitClient());
        return true;
    }
    case ZB_EGL_HC_eglWaitGL: {
        if (!call.valid()) return true;
        call.set_result(backend_.eglWaitGL());
        return true;
    }
    case ZB_EGL_HC_eglWaitNative: {
        const EGLint engine = call.scalar<EGLint>(0);
        if (!call.valid()) return true;
        call.set_result(backend_.eglWaitNative(engine));
        return true;
    }
    case ZB_EGL_HC_eglWaitSync: {
        EGLDisplay dpy = call.handle<EGLDisplay>(0);
        if (!call.valid()) return true;
        const EGLSync sync = call.scalar<EGLSync>(1);
        const EGLint flags = call.scalar<EGLint>(2);
        if (!call.valid()) return true;
        call.set_result(backend_.eglWaitSync(dpy, sync, flags));
        return true;
    }
    default:
        return false;
    }
}
